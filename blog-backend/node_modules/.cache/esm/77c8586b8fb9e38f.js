let jwt;_8fb‍.x([["default",()=>_8fb‍.o]]);_8fb‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleWare = (ctx, next) => {
  const token = ctx.cookies.get('access_token');
  if (!token) return next();
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    _8fb‍.g.console.log(decoded);
    return next();
  } catch (e) {
    return next();
  }
};

_8fb‍.d(jwtMiddleWare());
