let Koa,Router,bodyParser,mongoose,api,jwtMiddleware,createFakeData;_e60‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_e60‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_e60‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_e60‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_e60‍.w("./api",[["default",["api"],function(v){api=v}]]);_e60‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);_e60‍.w("./createFakeData",[["default",["createFakeData"],function(v){createFakeData=v}]]);require('dotenv').config();







// 비구조화 할당을 통하여 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
    // createFakeData();
  })
  .catch((e) => {
    _e60‍.g.console.log(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT 가 지정되어있지 않다면 4000 을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _e60‍.g.console.log('Listening to port %d', port);
});
